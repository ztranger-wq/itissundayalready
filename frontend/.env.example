# The base URL for the backend API.
# For local development with SAM, this should be http://127.0.0.1:3000
VITE_API_URL=http://127.0.0.1:3000/api

# After configuring Cognito with Google Sign-In, you will get this value.
# This is no longer used by the app code directly, but is kept here for reference.
# The Amplify configuration in aws-exports.js will handle it.
VITE_GOOGLE_CLIENT_ID=your-google-client-id
